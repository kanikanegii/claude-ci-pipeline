# Naming Conventions

- Use camelCase for variables and functions (`userId`, `fetchData()`)
- Use PascalCase for classes and components (`UserProfile`, `PaymentService`)
- Names should describe intent, not implementation (`isLoading` not `flag1`)
